import "./Reminder.scss";

export default function Reminder(reminder, key, darkmode) {
  return (
    <div className="reminder" key={key}>
      <h1>{name}</h1>
      <h2>{tabname}</h2>
    </div>
  );
}